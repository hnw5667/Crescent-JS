#!/usr/bin/env node
/**
 * Crescent.js CLI — bin entry point
 * 
 * This is the executable that gets installed globally via `npm install -g crescent-js`
 * or linked locally via `npm link`.
 * 
 * Usage: crescent run [app.js] [port]
 */

require('./run.js');